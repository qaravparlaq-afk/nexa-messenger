[package]
name = "nexa-gateway"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.8"
nexa-protocol = { path = "../core/protocol" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["macros", "rt-multi-thread", "net", "sync"] }
