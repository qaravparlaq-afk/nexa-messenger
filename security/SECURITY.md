# Security Policy

NEXA handles potentially sensitive communications.

Do not publish private keys, seed phrases, credentials, tokens or private user data in issues or commits.

A feature is not secure merely because it uses an encryption library. Review the complete protocol, key lifecycle, authentication, storage, transport, error handling and UI semantics.

Until a dedicated security contact is configured, do not disclose exploitable vulnerability details publicly.
